import projectVersions from "../../project-versions.json";

export const APP_VERSION = projectVersions.appVersion;
export const DATA_VERSION = projectVersions.dataVersion;
export const MOCK_SEED = projectVersions.mockSeed;
